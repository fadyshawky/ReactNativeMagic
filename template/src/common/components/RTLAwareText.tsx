import {Text} from 'react-native';

/**
 * Plain `Text`: in RTL React Native already right-aligns natural text and
 * swaps `textAlign: 'left' | 'right'` natively (see RTLAwareView).
 */
export const RTLAwareText = Text;
